# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (NEVER commit secrets)
.env
.env*.local
.env.production

# typescript
*.tsbuildinfo

# ---- SECURITY: never commit real financial data ----
# Raw source spreadsheets (used once locally for the importer)
*.xlsx
*.xls
/data/
# Derived JSON / DB dumps with real balances
*.dump
*.sql.gz
/backups/
db-backup-*.sql

# scratch
testwrite.tmp

# vercel
.vercel

# test / coverage
/coverage

# Vendor license signing key — NEVER commit
.nomadwealth-license-key.local
.claude/worktrees/
