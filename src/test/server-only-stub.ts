// Empty stub for the `server-only` package under vitest. In production, Next's
// bundler resolves the real marker (which errors if a server module is pulled
// into a client bundle); tests run in Node, so an empty module is correct.
export {};
